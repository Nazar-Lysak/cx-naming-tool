import styled from 'styled-components';

const Container = styled.div`
  grid-area: swiper;
  background: blue;
`;

const SwiperFilters = () => {
  return (
    <Container>
      <h2>Swiper Filters</h2>
    </Container>
  );
};

export default SwiperFilters;
